import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  container: {
    width: '100%',
  },
  container_margin: {
    paddingBottom: 12,
  },
}));

export { useStyles };
