import { styled } from '@material-ui/core';

const Card = styled('div')(() => ({
  backgroundColor: '#fff',
  boxShadow: '0 2px 4px rgba(0,0,0,0.5)',
  borderRadius: 4,
  padding: 12,
  width: '100%',
}));

export default Card;
